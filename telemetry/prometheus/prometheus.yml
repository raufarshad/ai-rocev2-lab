global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'gnmic'
    static_configs:
      - targets: ['10.0.0.232:9804'] ##change IP as needed
    metrics_path: /metrics
